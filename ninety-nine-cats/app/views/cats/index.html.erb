
<ul> 
    <% @cats.each do |cat| %>
    <li> <%= cat.name %> </li>
    <li> <%= cat.birth_date %> </li>
    <li> <%= cat.color %> </li>
    <li> <%= cat.sex %> </li>    
</ul>

<a href="cat_url(@cat)" > profile </a>