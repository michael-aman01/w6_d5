
<ul> 
    <% @cats.each do |cat| %>
        <br>
    <li> <%= cat.name %> </li>
    <li> <%= cat.birth_date %> </li>
    <li> <%= cat.color %> </li>

    <li> <%= cat.sex %> </li> 
     <a href="<%=cat_url %> " > profile </a>
        <br>
        <% end %>
</ul>


